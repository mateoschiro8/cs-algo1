#include "generador.h"
#include <iostream>
#include <fstream>
#include "vectores.h"
#include "entradaSalida.h"

using namespace std;

int main() {
    // funcion para generar automaticamente los archivos numericos
    // generarDatosLaboratorio(); // descomentar para crear automaticamente los archivos necesarios en la parte de entrada-salida. Una vez creados, se puede volver a comentar
    // holaModuloVectores(); // función definida en vectores.cpp

    vector<int> v = {21,114,87,34};
    int a = 2;
    string s = "leerVector.in";

    vector<int> w;

    // divideAux(v,a);
    // mayorAux(v);
    // reversoAux(v);
    // rotarAux(v, 3);
    // estaOrdenadoAux(v);
    // factoresPrimosAux();

    // w = leerVector(s);
    // mostrarVector(w);
    // guardarVector(v, "guardadoVectores");

    // elementoMedioAux("elementoMedio.in");
    // cantApariciones("10000NumerosEntre1y50.in");
    return 0;
}
